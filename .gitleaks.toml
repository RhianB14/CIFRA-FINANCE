title = "Cifra gitleaks configuration"

[extend]
useDefault = true

[allowlist]
description = "Fictitious local development marker only"
regexTarget = "match"
regexes = [
  '''cifra_local_development'''
]
