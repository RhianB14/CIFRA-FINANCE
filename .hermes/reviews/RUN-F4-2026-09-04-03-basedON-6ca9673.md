# Run RUN-F4-2026-09-04-03-basedON-6ca9673 — Expo e MinIO CI

- **Data:** 2026-09-04
- **Branch:** `feat/f4-credit-cards-invoices-installments`
- **Base main:** `cfdc4520379541aee90ad95c3481bd5ad0e3d803`
- **Head inicial:** `6ca96736dbcb75cfa10154bac1a7cb776eec381d`
- **Ledger de referência:** `.hermes/ledger/f4.md`, versão 2026-09-04 18:17:57 -03:00

## Pré-flight e guarda anti-reexecução

- `origin/main`: `cfdc4520379541aee90ad95c3481bd5ad0e3d803`.
- `origin/feat/f4-credit-cards-invoices-installments`: `6ca96736dbcb75cfa10154bac1a7cb776eec381d`.
- HEAD local igual ao remoto e working tree inicialmente limpa.
- Run ID ausente dos commits e do ledger; execução inédita.
- PR #21: `open`, `merged=false`, base `cfdc452`, head `6ca9673`.
- Check-runs iniciais: 11/13 success; falhas exclusivas em `Build and Expo` e `MinIO Image Scan`.

## Expo RED → GREEN

### RED

`pnpm --filter @cifra/mobile exec expo install --check` retornou exit 1:

- `expo@57.0.19` — esperado `~57.0.20`;
- `expo-router@57.0.18` — esperado `~57.0.19`.

### Alteração mínima

Raízes atualizadas somente em `apps/mobile/package.json`:

- `expo`: `57.0.19 → 57.0.20`;
- `expo-router`: `57.0.18 → 57.0.19`.

O lockfile foi regenerado com pnpm 11.24.0. As atualizações transitivas `@expo/cli 57.0.22`, `@expo/ui 57.0.16`, `expo-modules-core 57.0.16` e `expo-modules-jsi 57.0.8` são dependências obrigatórias declaradas pelos dois pacotes raiz atualizados, não ampliações independentes.

### GREEN

- `expo install --check`: exit 0, `Dependencies are up to date`.
- mobile lint: exit 0.
- mobile typecheck: exit 0.
- mobile validate: exit 0.
- mobile test: 1 suite, 2 testes, exit 0.

## MinIO RED → GREEN

### Reprodução RED

Trivy `v0.74.0`, imagem `minio/minio:RELEASE.2025-09-07T16-13-09Z`, HIGH/CRITICAL, `ignore-unfixed=false`, exit-code 1 e ignore existente encontrou exatamente um achado não ignorado:

| Identificador | Pacote | Instalada | Severidade | Fixed version | Status |
|---|---|---:|---|---:|---|
| `CVE-2026-79921` | `github.com/rabbitmq/amqp091-go` | `v1.10.0` | HIGH | `1.13.0` | fixed |

Exit real do scan RED: 1.

### Disponibilidade oficial

A API oficial do Docker Hub listou `RELEASE.2025-09-07T16-13-09Z` como a tag oficial mais nova de `minio/minio`. A página oficial de tags confirma o mesmo limite. Não existe imagem oficial mais nova que incorpore `amqp091-go 1.13.0`; não houve troca de pin.

### Exceção mínima

Adicionada somente `CVE-2026-79921 exp:2026-12-31` a `.trivyignore-minio`. A expiração não foi ampliada. A exceção continua restrita ao job `minio-scan` e à imagem MinIO local/dev sem exposição à Internet. Condição de remoção: imagem oficial MinIO que incorpore `github.com/rabbitmq/amqp091-go >= 1.13.0`.

A documentação foi reconciliada: Trivy sem ignore, ignore e tabela possuem os mesmos 58 IDs únicos, sendo 5 CRITICAL e 53 HIGH. A divergência documental preexistente de `CVE-2026-84304` foi reconciliada sem alterar seu ignore já existente.

### GREEN

O scan equivalente após a exceção terminou com exit real 0, sem reduzir severidades, `exit-code`, `ignore-unfixed`, versão do Trivy ou escopo do scan.

## Revisão independente

A primeira revisão apontou corretamente um arquivo fora do whitelist (`pnpm-workspace.yaml`) e questionou transitivas do lockfile. O arquivo fora do escopo foi integralmente revertido. Metadados oficiais dos pacotes comprovaram que as quatro transitivas são exigidas pelas duas raízes autorizadas.

A revisão independente final retornou **PASS** e confirmou:

- somente `.trivyignore-minio`, `apps/mobile/package.json`, `docs/security-exceptions.md` e `pnpm-lock.yaml` foram alterados no diff funcional, todos na whitelist;
- `pnpm-workspace.yaml`, workflows e `package.json` raiz permanecem inalterados;
- gates do MinIO permanecem Trivy v0.74.0, HIGH/CRITICAL, `exit-code: "1"`, `ignore-unfixed: false` e ignore exclusivo;
- lockfile restrito às duas raízes e quatro transitivas obrigatórias;
- somente `CVE-2026-79921` foi adicionada, com expiração preservada em 2026-12-31.

## Autorização humana para a política pnpm

Na continuação autorizada deste Run, o proprietário aprovou textualmente e de forma explícita a configuração permanente `minimumReleaseAge: 0` em `pnpm-workspace.yaml`, para remover a espera de 24 horas e manter o desenvolvimento contínuo.

Alteração realizada:

- adição exclusiva de uma linha em branco e `minimumReleaseAge: 0` ao final de `pnpm-workspace.yaml`;
- `minimumReleaseAgeExclude` preservado byte-for-byte, com as 14 entradas preexistentes;
- `allowBuilds` preservado byte-for-byte;
- nenhuma outra configuração do workspace alterada.

### Risco residual aceito

Com `minimumReleaseAge: 0`, versões recém-publicadas podem ser resolvidas e instaladas imediatamente, reduzindo a proteção temporal contra comprometimentos recentes de supply chain. O risco foi aceito explicitamente pelo proprietário. Permanecem ativos o frozen lockfile, a revisão do lockfile, `allowBuilds`, os scans de secrets e dependências, Trivy, lint, tipos, testes, coverage, build e CI.

## Revisão independente da alteração de política

O revisor independente read-only retornou **PASS**, confirmando por comparação contra o HEAD inicial:

- o diff de `pnpm-workspace.yaml` possui zero linhas removidas e somente a linha em branco mais `minimumReleaseAge: 0` adicionadas;
- `minimumReleaseAgeExclude` e `allowBuilds` estão byte-for-byte idênticos ao HEAD;
- todos os arquivos do diff pertencem à whitelist;
- lockfile restrito às seis versões autorizadas;
- única nova exceção MinIO é `CVE-2026-79921 exp:2026-12-31`;
- ignore e documentação contêm conjuntos idênticos de 58 IDs.

## Validação final da continuação autorizada

### Pré-flight

- `git fetch origin`: exit 0.
- `origin/main`: `cfdc4520379541aee90ad95c3481bd5ad0e3d803`.
- `origin/feat/f4-credit-cards-invoices-installments`: `6ca96736dbcb75cfa10154bac1a7cb776eec381d`.
- HEAD local inicial: `6ca96736dbcb75cfa10154bac1a7cb776eec381d`.
- Working tree correspondia exatamente ao WIP declarado, sem arquivo adicional.
- `git diff --check`: exit 0.

### Dependências e mobile

| Gate | Resultado |
|---|---|
| `pnpm install --frozen-lockfile` sem flag de override | exit 0; lockfile aprovado pela política |
| `expo install --check` | exit 0; dependências atualizadas |
| mobile lint | exit 0 |
| mobile typecheck | exit 0 |
| mobile validate | exit 0 |
| mobile test | exit 0; 1 suite e 2 testes passed |

Nenhum `ERR_PNPM_MINIMUM_RELEASE_AGE_VIOLATION` permaneceu.

### MinIO e Trivy

- Trivy `v0.74.0`, imagem `minio/minio:RELEASE.2025-09-07T16-13-09Z`, `HIGH,CRITICAL`, `ignore-unfixed=false`, `exit-code=1` e `--ignorefile /.trivyignore-minio`: exit real 0.
- Um primeiro comando de reconfirmação omitiu acidentalmente `--ignorefile` e retornou exit 1 com os achados já conhecidos; isso não foi interpretado como regressão. O comando foi corrigido para equivalência real com o workflow e passou.
- Scan JSON sem ignore: 58 IDs únicos, sendo 5 CRITICAL e 53 HIGH.
- `.trivyignore-minio`: 58 IDs únicos.
- `docs/security-exceptions.md`: 58 IDs únicos.
- Diferenças Trivy ↔ ignore ↔ documentação: zero.

### Gate integral

`pnpm verify` foi executado serialmente. A primeira tentativa encontrou apenas o artefato diagnóstico não rastreado `f4_03_trivy_noignore.json` na raiz durante `prettier --check`; o arquivo foi movido para `%LOCALAPPDATA%/Temp`, restaurando exatamente a whitelist. A reexecução autoritativa terminou com exit 0:

- lint/ruff: pass;
- mypy strict: zero issues em 143 arquivos;
- suíte API integral: 441 passed;
- cobertura API: 441 passed; cobertura global 84%; serviços 90%;
- mobile: 2/2 testes;
- web: 16/16 testes;
- api-client: 6/6 testes;
- build: pass;
- secrets de arquivos e histórico: pass;
- Expo check: dependências atualizadas;
- Expo Doctor: 21/21 checks passed.

A suíte API integral está completamente coberta pelo próprio `pnpm verify`, portanto não foi duplicada em um terceiro pytest isolado.

### Compose e smokes

- `docker compose up -d --build --wait`: exit 0.
- Serviços `api`, `web`, `postgres`, `redis` e `minio`: 5/5 `running` e `healthy`.
- F2-SMOKE: exit 0, `F2-SMOKE-OK`.
- F3-SMOKE: exit 0, `F3-SMOKE-OK`.
- F4-SMOKE: exit 0, `F4-SMOKE-OK`.

### Inspeção final pré-commit

- `git diff --check`: exit 0.
- Arquivos funcionais/configuração no diff: exatamente `.trivyignore-minio`, `apps/mobile/package.json`, `docs/security-exceptions.md`, `pnpm-lock.yaml` e `pnpm-workspace.yaml`.
- Relatório: único arquivo não rastreado permitido.
- Nenhum workflow, código funcional, teste, modelo, router ou migração foi alterado.

## Critérios e confidence

| Critério | Estado | Confidence |
|---|---|---|
| Política `minimumReleaseAge: 0` exclusiva e autorizada | PROVEN | HIGH |
| `minimumReleaseAgeExclude` e `allowBuilds` preservados | PROVEN | HIGH |
| Frozen install natural | PROVEN | HIGH |
| Lockfile restrito às seis versões autorizadas | PROVEN | HIGH |
| Expo check e gates mobile | PROVEN | HIGH |
| Trivy equivalente e igualdade dos 58 IDs | PROVEN | HIGH |
| Revisão independente da política | PROVEN | HIGH |
| `pnpm verify` e suíte API integral | PROVEN | HIGH |
| Compose 5/5 healthy | PROVEN | HIGH |
| Smokes F2/F3/F4 | PROVEN | HIGH |
| Push e 13/13 no mesmo HEAD | PENDING | HIGH |
| PR aberto/não mergeado e main inalterada | PENDING pós-push | HIGH |

## Known unknowns

- O resultado do CI remoto no novo HEAD ainda será comprovado após os dois commits e o push normal.

## Non-verifiable claims

- Nenhuma no estado local: todas as aprovações locais declaradas acima vieram de comandos executados pelo Hermes principal e outputs reais.

## Estado remoto e commits

- Commit funcional: `c574cbc` — `fix(ci): unblock Expo and MinIO checks`; contém exclusivamente os cinco arquivos de configuração/dependências autorizados.
- Commit dedicado do relatório: este documento será commitado imediatamente após esta atualização.
- Push e CI: pendentes; o resultado remoto será reconciliado em atualização documental posterior sem alterar o conteúdo funcional.

VEREDITO: PARCIAL — todos os gates locais e a revisão independente estão verdes; push e CI 13/13 ainda pendentes.
MERGE: não (não autorizado neste prompt)
